# Commit lint table: single source of truth for .githooks/commit-msg.
# Documented in CONTRIBUTING.md「提交信息规范」; change here, not in the hook.
# Format: `type <value>` or `verb <value>`, one per line, '#' starts a comment.

type feat
type fix
type refactor
type docs
type test
type chore
type perf
type build
type revert

verb add
verb accept
verb adapt
verb adjust
verb align
verb allow
verb avoid
verb build
verb change
verb check
verb clarify
verb clean
verb convert
verb correct
verb cover
verb create
verb cut
verb delete
verb disable
verb document
verb drop
verb enable
verb ensure
verb expand
verb expose
verb fix
verb forward
verb generate
verb guard
verb handle
verb harden
verb ignore
verb implement
verb improve
verb include
verb init
verb integrate
verb introduce
verb keep
verb limit
verb load
verb log
verb mark
verb merge
verb migrate
verb move
verb normalize
verb pin
verb preserve
verb prevent
verb record
verb reduce
verb refactor
verb reject
verb release
verb remove
verb rename
verb reorder
verb replace
verb report
verb reset
verb resolve
verb restrict
verb restore
verb return
verb revert
verb rewrite
verb set
verb simplify
verb split
verb support
verb sync
verb test
verb tighten
verb unify
verb update
verb use
verb validate
verb verify
verb wrap
