[package]
name = "guard"
version = "0.1.0"
edition = "2021"
publish = false
description = "Repository guards for Stasis: docs, line width, policy"

[[bin]]
name = "guard"
path = "src/main.rs"

[dependencies]
